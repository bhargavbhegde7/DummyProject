apiVersion: v1
kind: Service
metadata:
  name: dummyproject-service
  namespace: dummyproject
  labels:
    app: dummyproject
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: dummyproject

